/* eslint-disable @typescript-eslint/no-explicit-any */
// Placeholder voor de gegenereerde Supabase-types. Tot er een lokale database
// is gebruiken we `any`, zodat de Supabase-client elke tabel/RPC accepteert.
// VERVANG dit bestand zodra de database draait met:
//   npm run db:types   (= supabase gen types typescript --local > types/database.ts)
// Dat levert strikte, schema-correcte types voor tabellen, RPC's en enums.
export type Database = any;
